import React from "react";
import s from "./Login.module.css";

const Login = props => {
  return (
    <div className={s.login}>
      <h1>Login</h1>
    </div>
  );
};

export default Login;
